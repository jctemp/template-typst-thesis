#let abstract_de() = {
  v(1fr)
  
  align(
    center, 
    text(
      weight: "semibold", 
      smallcaps("Zusammenfassung")
    )
  )

  text(lang: "de")[
    Note: Insert the German translation of the English abstract here.
  ]
  
  v(1fr)

  pagebreak()
}